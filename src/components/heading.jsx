import React, { Component } from 'react'

export default class heading extends Component {  // class name extends component name , Component is a builtin class in react package (imports)
  render() {
    return (
      <div>heading</div>
    )
  }
}
